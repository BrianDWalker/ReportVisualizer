# v1.2.1 refinement verification

Date: 2026-08-20

## Automated verification

- `./mvnw clean verify`: 130 tests, zero failures/errors/skips; all five Tycho
  reactor modules succeeded.
- `./scripts/build-update-site.sh`: succeeded and validated a latest-only p2
  repository containing one `1.2.1.202608210150` plug-in and feature.
- Date hierarchy tests cover direct Original, Year, Quarter, Month, and Day
  selection, the full drill-down/drill-up sequence, and immutable projection.
- Slicer tests cover Select Values availability for numeric/date fields,
  predicate availability, SQL-style `<>`, and case-insensitive existing-slicer
  lookup. Snapshot tests retain numeric/date predicate behavior.
- The complete preset regression saves chart/Matrix fields, multiple Values,
  aggregation, Y Max, chart/Matrix options, distinct-value and numeric/date
  predicate slicers, date hierarchy, sorts, and calculated fields; it then
  constructs radically different state and confirms the saved state restores
  exactly. Preset format remains v6 with v2-v5 read compatibility.

## Live DBeaver validation

The candidate was installed into the existing isolated writable DBeaver
Community 26.1.4 copy and isolated workspace; the normal application and
workspace were not modified. Equinox reported bundle 369 ACTIVE at
`1.2.1.202608210150`. A SQLite Invoice query returned five fields and 50 rows,
rendered a chart, and showed the compact three-row builder with Export retained
above the chart and `Date: Original ▾` for the active InvoiceDate X field.

The live layout confirmed no permanent sidebar and no material chart-area
loss. Operator lists, edit preload/replacement, date transitions, silent Copy
Image behavior, and the exact preset round trip are additionally guarded by
the automated model/UI regression tests described above.

## Dependency and resource audit

No dependency was added. The runtime bundle contains only plug-in classes and
resources, with no embedded JAR or native library. Clipboard ownership remains
nested and deterministic, and captured SWT images are disposed on success and
failure. No process launching, scripting engine, socket server, or new network
surface was introduced.
